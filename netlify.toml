[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  SECRETS_SCAN_OMIT_KEYS = "NEXT_PUBLIC_SUPABASE_ANON_KEY"

# Next.js middleware (auth) deluje kot Netlify Edge Functions
[[plugins]]
  package = "@netlify/plugin-nextjs"
